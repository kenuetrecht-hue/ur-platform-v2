/**
 * Stickers Tab
 * Entry point for the sticker store tab
 */

import { useRouter } from 'expo-router';
import StickerStoreScreen from '../sticker-store';

export default function StickersTab() {
  return <StickerStoreScreen />;
}
